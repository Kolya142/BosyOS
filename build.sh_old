set -e
cd bootloader
nasm main.asm
cd ../kernel

python build.py

cd ..
cat bootloader/main kernel/boot/boot kernel/kernel.b > drive
qemu-system-i386 -drive format=raw,file=drive # -S -s &
# (echo -e '\n\n\n\ntarget remote localhost:1234'; cat) | gdb os.bin
